import { AlbumArtistsPipe } from './album-artists.pipe';

describe('AlbumArtistsPipe', () => {
  it('create an instance', () => {
    const pipe = new AlbumArtistsPipe();
    expect(pipe).toBeTruthy();
  });
});
